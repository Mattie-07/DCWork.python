#3. Friend counter
# Using the dictionary from the Nested dictionaries exercise above, write a function countFriends() that accepts
#  a dictionary as an argument and returns a new dictionary that includes a new key


def countFriends(newDictionary):
    if newDictionary !=type(list)
        print("Sorry. You can't use a dictonary or list to create a new key")
    return newDictionary = {}
